const expect = require('chai').expect();

const DocXml = require('../serialization/DocXml');

describe("DocXml", function() {

  describe("convertDocument", function() {

    it("Can import the sample doc as it is serialized to JSON by default", function() {

    });

  })

});
